{
  "name": "skills-mine-mock",
  "version": "1.0.0",
  "description": "Mock API server for SkillsMine frontend development",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "mock": "node server.js",
    "mock:watch": "node --watch server.js"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.15",
    "lowdb": "^7.0.1"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
