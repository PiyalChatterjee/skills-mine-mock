openapi: 3.0.3
info:
  title: SkillsMine – Candidate API
  description: >
    Canonical v0 contract for all candidate-facing endpoints served by the
    SkillsMine mock server.  Covers the candidate landing page, dashboard,
    profile, CV builder, resume preview/download, recommended positions,
    saved jobs, AI actions, CV upload, candidate list, candidate search,
    and the recruiter candidate-profile view.
  version: "0.1.0"
  contact:
    name: SkillsMine Mock Server
    url: http://localhost:4000

servers:
  - url: http://localhost:4000
    description: Local mock server

tags:
  - name: Candidate Landing
    description: Public landing-page summary for the candidate portal
  - name: Candidate Dashboard
    description: Authenticated candidate home dashboard
  - name: Candidate Profile
    description: Candidate self-service profile view and recruiter profile view
  - name: CV Build
    description: Build My CV – create, retrieve, and update a structured CV
  - name: Resume
    description: Resume preview and download URL generation
  - name: Recommended Positions
    description: AI-powered job recommendations for candidates
  - name: Saved Jobs
    description: Candidate saved / bookmarked jobs
  - name: AI Actions
    description: AI-triggered candidate actions (e.g. matched-jobs mailer)
  - name: CV Upload
    description: CV upload and extraction for an application
  - name: Candidate List
    description: Paginated, filterable list of all candidates (recruiter/admin)
  - name: Candidate Search
    description: Recruiter ATS candidate search

security:
  - bearerAuth: []

# ─────────────────────────────────────────────────────────────────────────────
paths:

  # ── Landing ────────────────────────────────────────────────────────────────
  /candidates/landing:
    get:
      tags: [Candidate Landing]
      summary: Get candidate landing-page summary
      description: >
        Returns aggregate platform stats and a preview list of open positions
        to display on the unauthenticated (or pre-login) candidate landing page.
      operationId: getCandidateLanding
      security: []
      responses:
        '200':
          description: Landing-page data retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CandidateLanding'

  # ── Dashboard ──────────────────────────────────────────────────────────────
  /candidates/dashboard:
    get:
      tags: [Candidate Dashboard]
      summary: Get candidate dashboard
      description: >
        Returns summary counts, recent activity, top-5 application cards with
        pipeline stage, and quick-link shortcuts for the authenticated candidate.
      operationId: getCandidateDashboard
      responses:
        '200':
          description: Dashboard data retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CandidateDashboard'
        '401':
          $ref: '#/components/responses/Unauthorized'

  # ── Candidate Profile ──────────────────────────────────────────────────────
  /candidates/profile/:
    get:
      tags: [Candidate Profile]
      summary: Get authenticated candidate's own profile
      description: >
        Returns the full profile for the currently authenticated candidate.
        Equivalent to the recruiter profile view but scoped to self.
      operationId: getCandidateProfile
      responses:
        '200':
          description: Candidate profile retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CandidateProfileFull'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  # ── CV Build ───────────────────────────────────────────────────────────────
  /candidates/cv-build/:
    get:
      tags: [CV Build]
      summary: Get Build My CV data
      description: >
        Returns the current user's saved Build My CV state. Never errors —
        returns an empty template when no record exists yet.
      operationId: getCvBuild
      responses:
        '200':
          description: CV builder data retrieved (or empty template returned)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CvBuilderRecord'
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      tags: [CV Build]
      summary: Create or update Build My CV
      description: >
        Creates a new Build My CV record (HTTP 201) when none exists, or
        updates the existing one (HTTP 200). Only the CV sections present in
        the request body are written; absent sections retain their previous
        values.
      operationId: postCvBuild
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CvBuilderBody'
      responses:
        '200':
          description: Build My CV updated successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CvBuilderRecord'
        '201':
          description: Build My CV created successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CvBuilderRecord'
        '401':
          $ref: '#/components/responses/Unauthorized'

    put:
      tags: [CV Build]
      summary: Partially update Build My CV
      description: >
        Partially updates an existing Build My CV record. Only sections present
        in the request body are overwritten; the rest are preserved. If no
        record exists, one is created automatically.
      operationId: putCvBuild
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CvBuilderBody'
      responses:
        '200':
          description: Build My CV updated successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CvBuilderRecord'
        '401':
          $ref: '#/components/responses/Unauthorized'

  # ── Recommended Positions ──────────────────────────────────────────────────
  /candidates/recommended-positions:
    get:
      tags: [Recommended Positions]
      summary: Get AI-recommended positions for the authenticated candidate
      description: >
        Returns up to 6 open jobs ranked by a computed match score based on
        skill overlap with the candidate's profile. Resolves the candidateId
        from the bearer token.
      operationId: getRecommendedPositions
      responses:
        '200':
          description: Recommended positions retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/RecommendedPositionsResult'
        '401':
          $ref: '#/components/responses/Unauthorized'

  # ── Saved Jobs ─────────────────────────────────────────────────────────────
  /candidates/saved-jobs:
    get:
      tags: [Saved Jobs]
      summary: Get saved / bookmarked jobs for the authenticated candidate
      description: >
        Returns all jobs that the authenticated candidate has saved.
      operationId: getSavedJobs
      responses:
        '200':
          description: Saved jobs retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/SavedJobsResult'
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      tags: [Saved Jobs]
      summary: Save / bookmark a job
      description: >
        Adds a job to the authenticated candidate's saved-jobs list.
        Idempotent — saving an already-saved job returns 200 without error.
      operationId: saveJob
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SaveJobRequest'
      responses:
        '200':
          description: Job saved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessEnvelope'
        '400':
          description: Invalid request body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /candidates/saved-jobs/{jobId}:
    delete:
      tags: [Saved Jobs]
      summary: Remove a saved / bookmarked job
      description: >
        Removes a job from the authenticated candidate's saved-jobs list.
        Idempotent — removing a job that was never saved returns 200 without error.
      operationId: unsaveJob
      parameters:
        - name: jobId
          in: path
          required: true
          schema:
            type: string
            example: j001
          description: ID of the job to remove from saved jobs
      responses:
        '200':
          description: Job removed from saved jobs
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'

  # ── AI Actions ─────────────────────────────────────────────────────────────
  /candidates/ai-actions/:
    get:
      tags: [AI Actions]
      summary: Get available AI actions for the authenticated candidate
      description: >
        Returns a list of AI-driven actions available to the candidate,
        such as match-score summaries, suggested skills, and career nudges.
      operationId: getCandidateAiActions
      responses:
        '200':
          description: AI actions retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/AiActionsResult'
        '401':
          $ref: '#/components/responses/Unauthorized'

  # ── CV Upload ──────────────────────────────────────────────────────────────
  /applications/{applicationId}/cv/upload:
    post:
      tags: [CV Upload]
      summary: Upload and parse a CV for an application
      description: >
        Accepts a multipart CV upload (mock only — does not require a real
        file). Returns the extracted CV data.
      operationId: uploadApplicationCv
      parameters:
        - $ref: '#/components/parameters/ApplicationId'
      requestBody:
        required: false
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: CV file (PDF or DOCX)
      responses:
        '200':
          description: CV uploaded and parsed successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CvUploadResult'
        '401':
          $ref: '#/components/responses/Unauthorized'

  # ── Candidate List (recruiter/admin) ───────────────────────────────────────
  /candidates:
    get:
      tags: [Candidate List]
      summary: List all candidates (paginated and filterable)
      description: >
        Returns a paginated list of all candidates. Supports optional filtering
        by name/email (`search`), location substring, and skill substring.
      operationId: listCandidates
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
          description: Page number (1-based)
        - name: limit
          in: query
          schema:
            type: integer
          description: Number of results per page (omit for all results)
        - name: search
          in: query
          schema:
            type: string
          description: Filter by full name or email (case-insensitive substring)
        - name: location
          in: query
          schema:
            type: string
          description: Filter by location (case-insensitive substring)
        - name: skill
          in: query
          schema:
            type: string
          description: Filter by skill name (case-insensitive substring)
      responses:
        '200':
          description: Candidates retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CandidateListResult'
        '401':
          $ref: '#/components/responses/Unauthorized'

  # ── Recruiter Candidate Search ─────────────────────────────────────────────
  /recruiter/candidates/search:
    get:
      tags: [Candidate Search]
      summary: ATS candidate search (recruiter)
      description: >
        Searches candidate profiles by skill and/or EE status.
      operationId: recruiterCandidateSearch
      parameters:
        - name: skill
          in: query
          schema:
            type: string
          description: Filter by skill name (case-insensitive substring)
        - name: eeStatus
          in: query
          schema:
            type: string
          description: Filter by EE status (e.g. "African Male")
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
      responses:
        '200':
          description: Candidates retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/RecruiterCandidateSearchResult'
        '401':
          $ref: '#/components/responses/Unauthorized'

  # ── Recruiter Candidate Profile ────────────────────────────────────────────
  /api/v1/candidates/{candidateId}/profile:
    get:
      tags: [Candidate Profile]
      summary: Get full candidate profile (recruiter view)
      description: >
        Returns the full candidate profile as viewed by a recruiter.
      operationId: getRecruiterCandidateProfile
      parameters:
        - $ref: '#/components/parameters/CandidateId'
      responses:
        '200':
          description: Candidate profile retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/RecruiterCandidateProfile'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'

  # ── Legacy path aliases (kept for backwards compatibility) ─────────────────
  /candidate/{userId}/dashboard:
    get:
      tags: [Candidate Dashboard]
      summary: Get candidate dashboard (legacy — prefer /candidates/dashboard)
      operationId: getCandidateDashboardLegacy
      deprecated: true
      parameters:
        - $ref: '#/components/parameters/UserId'
      responses:
        '200':
          description: Dashboard data retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CandidateDashboard'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /candidate/buildmycv:
    get:
      tags: [CV Build]
      summary: Get Build My CV data (legacy — prefer /candidates/cv-build/)
      operationId: getCvBuilderLegacy
      deprecated: true
      responses:
        '200':
          description: CV builder data retrieved
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CvBuilderRecord'
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      tags: [CV Build]
      summary: Create or update Build My CV (legacy)
      operationId: postCvBuilderLegacy
      deprecated: true
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CvBuilderBody'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CvBuilderRecord'
        '201':
          description: Created
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CvBuilderRecord'
        '401':
          $ref: '#/components/responses/Unauthorized'

    put:
      tags: [CV Build]
      summary: Partially update Build My CV (legacy)
      operationId: putCvBuilderLegacy
      deprecated: true
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CvBuilderBody'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CvBuilderRecord'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /candidate/{resumeId}/preview:
    get:
      tags: [Resume]
      summary: Get CV preview URL (legacy — prefer /candidates/cv-build/)
      operationId: getResumePreviewLegacy
      deprecated: true
      parameters:
        - $ref: '#/components/parameters/ResumeId'
      responses:
        '200':
          description: Preview URL generated
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ResumePreview'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /candidate/{resumeId}/download:
    get:
      tags: [Resume]
      summary: Get CV download URL (legacy)
      operationId: getResumeDownloadLegacy
      deprecated: true
      parameters:
        - $ref: '#/components/parameters/ResumeId'
      responses:
        '200':
          description: Download URL generated
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ResumeDownload'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /candidate/{candidateId}/recommended-jobs:
    get:
      tags: [Recommended Positions]
      summary: Get recommended jobs by candidateId (legacy — prefer /candidates/recommended-positions)
      operationId: getRecommendedJobsLegacy
      deprecated: true
      parameters:
        - $ref: '#/components/parameters/CandidateId'
      responses:
        '200':
          description: Recommended jobs retrieved
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/RecommendedPositionsResult'
        '401':
          $ref: '#/components/responses/Unauthorized'

# ─────────────────────────────────────────────────────────────────────────────
components:

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  # ── Reusable parameters ────────────────────────────────────────────────────
  parameters:
    UserId:
      name: userId
      in: path
      required: true
      schema:
        type: string
        example: USR100001
      description: User ID or the literal string `me` to resolve to the authenticated user

    CandidateId:
      name: candidateId
      in: path
      required: true
      schema:
        type: string
        example: CAND100001
      description: Unique candidate identifier

    ResumeId:
      name: resumeId
      in: path
      required: true
      schema:
        type: string
        example: RES100001
      description: Unique resume identifier

    ApplicationId:
      name: applicationId
      in: path
      required: true
      schema:
        type: string
        example: APP-MS-001
      description: Unique application identifier

  # ── Reusable responses ─────────────────────────────────────────────────────
  responses:
    Unauthorized:
      description: Missing or invalid Bearer token
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            success: false
            statusCode: 401
            message: Unauthorized

    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
          example:
            success: false
            statusCode: 404
            message: Resource not found.

  # ── Schemas ────────────────────────────────────────────────────────────────
  schemas:

    # ── Shared envelopes ─────────────────────────────────────────────────────
    SuccessEnvelope:
      type: object
      required: [success, statusCode, message]
      properties:
        success:
          type: boolean
          example: true
        statusCode:
          type: integer
          example: 200
        message:
          type: string

    ErrorEnvelope:
      type: object
      required: [success, statusCode, message]
      properties:
        success:
          type: boolean
          example: false
        statusCode:
          type: integer
          example: 404
        message:
          type: string

    Pagination:
      type: object
      required: [page, pageSize, total, totalPages]
      properties:
        page:
          type: integer
          example: 1
        pageSize:
          type: integer
          example: 20
        total:
          type: integer
          example: 87
        totalPages:
          type: integer
          example: 5

    # ── Pipeline enums ────────────────────────────────────────────────────────
    PipelineStageEnum:
      type: string
      enum: [APPLIED, SCREENING, ASSESSMENT, INTERVIEW, SHORTLISTED, OFFER]

    InternalStageEnum:
      type: string
      enum: [Inbound, Screening, Assessment, Interview, Shortlisted, Offer, Placed, Closed]

    # ── Shared building blocks ────────────────────────────────────────────────
    LanguageEntry:
      type: object
      required: [language, proficiency]
      properties:
        language:
          type: string
          example: English
        proficiency:
          type: string
          example: Native

    SecondaryEducation:
      type: object
      required: [schoolName, qualification, yearCompleted]
      properties:
        schoolName:
          type: string
          example: High School of Gauteng
        qualification:
          type: string
          example: Grade 12
        yearCompleted:
          type: integer
          example: 2012

    TertiaryEducation:
      type: object
      required: [institution, qualification]
      properties:
        institution:
          type: string
          example: University of the Witwatersrand
        qualification:
          type: string
          example: BSc Computer Science
        fieldOfStudy:
          type: string
          example: Computer Science
        yearCompleted:
          type: integer
          nullable: true
          example: 2016

    EducationRecord:
      type: object
      properties:
        secondaryEducation:
          type: array
          items:
            $ref: '#/components/schemas/SecondaryEducation'
        tertiaryEducation:
          type: array
          items:
            $ref: '#/components/schemas/TertiaryEducation'

    CareerHistoryEntry:
      type: object
      required: [company, jobTitle, startDate]
      properties:
        company:
          type: string
          example: Accenture
        jobTitle:
          type: string
          example: Senior Software Engineer
        startDate:
          type: string
          example: "2020-03"
        endDate:
          type: string
          nullable: true
          example: "Present"
        responsibilities:
          type: string
          example: Lead frontend development using React and Node.js.

    ExperienceEntry:
      allOf:
        - $ref: '#/components/schemas/CareerHistoryEntry'
        - type: object
          properties:
            experienceId:
              type: string
              example: exp-001
            current:
              type: boolean
              example: true
            skills:
              type: array
              items:
                type: string
              example: [React, Node.js, TypeScript, AWS]

    PersonalDetails:
      type: object
      required: [firstName, lastName, email]
      properties:
        firstName:
          type: string
          example: Michael
        lastName:
          type: string
          example: Smith
        email:
          type: string
          format: email
          example: michael.smith@email.com
        mobileNumber:
          type: string
          example: "+27821234567"
        race:
          type: string
          example: African
        gender:
          type: string
          example: Male
        disabilityStatus:
          type: string
          example: None
        nationality:
          type: string
          example: South African
        location:
          type: string
          example: Johannesburg
        currentCompany:
          type: string
          example: Accenture
        currentPosition:
          type: string
          example: Senior Software Engineer
        noticePeriod:
          type: string
          example: 1 month
        idNumber:
          type: string
          example: "9001015800086"
        eeStatus:
          type: string
          nullable: true
          example: African Male
        profileImageUrl:
          type: string
          example: https://mock-cdn.skillsmine.com/profiles/USR100001/photo.jpg
        thumbnailUrl:
          type: string
          example: https://mock-cdn.skillsmine.com/profiles/USR100001/thumb.jpg
        linkedinUrl:
          type: string
          example: https://linkedin.com/in/michael-smith-dev
        portfolioUrl:
          type: string
          example: ""

    DesiredJob:
      type: object
      properties:
        jobTitle:
          type: string
          example: Senior Software Engineer
        jobType:
          type: string
          example: Permanent
        workType:
          type: string
          example: Hybrid
        salaryExpectation:
          type: number
          example: 85000
        currency:
          type: string
          example: ZAR
        availableFrom:
          type: string
          format: date
          example: "2025-02-01"
        industries:
          type: array
          items:
            type: string
          example: [Technology, Financial Services]
        locations:
          type: array
          items:
            type: string
          example: [Johannesburg, Remote]

    # ── Landing ───────────────────────────────────────────────────────────────
    CandidateLanding:
      type: object
      required: [stats, featuredJobs]
      properties:
        stats:
          type: object
          required: [totalJobs, totalCandidates, totalPlacements]
          properties:
            totalJobs:
              type: integer
              example: 42
            totalCandidates:
              type: integer
              example: 312
            totalPlacements:
              type: integer
              example: 87
        featuredJobs:
          type: array
          items:
            $ref: '#/components/schemas/FeaturedJob'

    FeaturedJob:
      type: object
      required: [jobId, title, company, location, workType, employmentType]
      properties:
        jobId:
          type: string
          example: j001
        title:
          type: string
          example: Senior React Developer
        company:
          type: string
          example: Standard Bank
        location:
          type: string
          example: Johannesburg, Gauteng
        workType:
          type: string
          example: Hybrid
        employmentType:
          type: string
          example: Permanent
        salaryRange:
          type: string
          example: R70 000 – R95 000
        skills:
          type: array
          items:
            type: string
          example: [React, TypeScript]
        postedDate:
          type: string
          format: date
          example: "2024-10-15"

    # ── Dashboard ─────────────────────────────────────────────────────────────
    DashboardApplicationCard:
      type: object
      required: [id, job, stage, statusMessage, pipeline]
      properties:
        id:
          type: string
          example: APP-MS-001
        job:
          type: object
          required: [id, title, company]
          properties:
            id:
              type: string
              example: j001
            title:
              type: string
              example: Senior React Developer
            company:
              type: string
              example: Standard Bank
        stage:
          $ref: '#/components/schemas/PipelineStageEnum'
        statusMessage:
          type: string
          example: You have been selected for an interview.
        pipeline:
          type: array
          items:
            $ref: '#/components/schemas/PipelineStageEnum'

    CandidateDashboard:
      type: object
      required: [id, candidateId, summary, activity, applications, quickLinks]
      properties:
        id:
          type: string
          description: User ID
          example: USR100001
        candidateId:
          type: string
          nullable: true
          example: CAND100001
        summary:
          type: object
          required: [totalApplications, submitted, inProgress, successful]
          properties:
            totalApplications:
              type: integer
              example: 3
            submitted:
              type: integer
              example: 0
            inProgress:
              type: integer
              example: 2
            successful:
              type: integer
              example: 0
        activity:
          type: object
          required: [jobsAppliedThisWeek, recruiterProfileViewsThisWeek, coursesCompletedThisWeek]
          properties:
            jobsAppliedThisWeek:
              type: integer
              example: 2
            recruiterProfileViewsThisWeek:
              type: integer
              example: 3
            coursesCompletedThisWeek:
              type: integer
              example: 0
        applications:
          type: array
          maxItems: 5
          items:
            $ref: '#/components/schemas/DashboardApplicationCard'
        quickLinks:
          type: array
          items:
            type: string
          example: [CV_BUILDER, SAVED_JOBS, LATEST_JOBS, RECOMMENDED_JOBS]

    # ── Candidate Self-Profile ────────────────────────────────────────────────
    CandidateProfileFull:
      type: object
      required: [candidateId, userId, personalDetails]
      properties:
        candidateId:
          type: string
          example: CAND100001
        userId:
          type: string
          example: USR100001
        accountStatus:
          type: string
          example: ACTIVE
        profileCompleted:
          type: integer
          minimum: 0
          maximum: 100
          example: 85
        personalDetails:
          $ref: '#/components/schemas/PersonalDetails'
        desiredJob:
          $ref: '#/components/schemas/DesiredJob'
        education:
          $ref: '#/components/schemas/EducationRecord'
        experience:
          type: array
          items:
            $ref: '#/components/schemas/ExperienceEntry'
        skills:
          type: array
          items:
            type: string
          example: [React, Node.js, TypeScript, AWS]
        languages:
          type: array
          items:
            $ref: '#/components/schemas/LanguageEntry'
        resume:
          $ref: '#/components/schemas/ResumeRef'
          nullable: true
        applications:
          type: array
          items:
            $ref: '#/components/schemas/ApplicationSummary'
        savedJobs:
          type: array
          items:
            type: string
          description: Array of saved job IDs
          example: [j001, j003]

    # ── Application summary ───────────────────────────────────────────────────
    ApplicationSummary:
      type: object
      required: [applicationId, jobId, jobTitle, company, currentStage, appliedDate, matchScore]
      properties:
        applicationId:
          type: string
          example: APP-MS-001
        jobId:
          type: string
          example: j001
        mandateId:
          type: string
          nullable: true
          example: MND001
        recruiterId:
          type: string
          nullable: true
          example: r001
        jobTitle:
          type: string
          example: Senior React Developer
        company:
          type: string
          example: Standard Bank
        currentStage:
          $ref: '#/components/schemas/InternalStageEnum'
        appliedDate:
          type: string
          format: date
          example: "2024-10-16"
        updatedAt:
          type: string
          format: date-time
          example: "2024-11-10T09:00:00Z"
        matchScore:
          type: integer
          minimum: 0
          maximum: 100
          example: 92

    # ── CV Builder ────────────────────────────────────────────────────────────
    CvBuilderPersonalDetails:
      type: object
      properties:
        firstName:
          type: string
          example: Michael
        lastName:
          type: string
          example: Smith
        race:
          type: string
          example: African
        gender:
          type: string
          example: Male
        disabilityStatus:
          type: string
          example: None
        nationality:
          type: string
          example: South African
        location:
          type: string
          example: Johannesburg
        currentCompany:
          type: string
          example: Accenture
        currentPosition:
          type: string
          example: Senior Software Engineer
        noticePeriod:
          type: string
          example: 1 month

    CvBuilderBody:
      type: object
      description: >
        Partial update body. Only the sections present are merged;
        absent sections are left unchanged.
      properties:
        personalDetails:
          $ref: '#/components/schemas/CvBuilderPersonalDetails'
        careerHistory:
          type: array
          items:
            $ref: '#/components/schemas/CareerHistoryEntry'
        skills:
          type: array
          items:
            type: string
          example: [React, Node.js, TypeScript]
        education:
          $ref: '#/components/schemas/EducationRecord'
        languages:
          type: array
          items:
            $ref: '#/components/schemas/LanguageEntry'
        extractionStatus:
          type: string
          enum: [NOT_STARTED, IN_PROGRESS, COMPLETED]
          example: COMPLETED
        validation:
          type: array
          items:
            type: object

    CvBuilderRecord:
      allOf:
        - $ref: '#/components/schemas/CvBuilderBody'
        - type: object
          required: [userId, source, extractionStatus, lastModified, createdAt]
          properties:
            userId:
              type: string
              example: USR100001
            source:
              type: string
              example: BuildCV
            createdAt:
              type: string
              format: date-time
              nullable: true
              example: "2024-11-01T14:30:00Z"
            lastModified:
              type: string
              format: date-time
              nullable: true
              example: "2024-11-05T09:15:00Z"

    # ── Resume preview / download ─────────────────────────────────────────────
    ResumePreview:
      type: object
      required: [resumeId, previewUrl, generatedAt, expiresIn]
      properties:
        resumeId:
          type: string
          example: RES100001
        previewUrl:
          type: string
          format: uri
          example: https://mock-cdn.skillsmine.com/resumes/RES100001/preview.pdf
        generatedAt:
          type: string
          format: date-time
          example: "2025-01-15T10:00:00Z"
        expiresIn:
          type: integer
          description: Seconds until the URL expires
          example: 3600

    ResumeDownload:
      type: object
      required: [resumeId, downloadUrl, filename, generatedAt, expiresIn]
      properties:
        resumeId:
          type: string
          example: RES100001
        downloadUrl:
          type: string
          format: uri
          example: https://mock-cdn.skillsmine.com/resumes/RES100001/download.pdf
        filename:
          type: string
          example: Michael-Smith-Senior-Software-Engineer-CV.pdf
        generatedAt:
          type: string
          format: date-time
          example: "2025-01-15T10:00:00Z"
        expiresIn:
          type: integer
          description: Seconds until the URL expires
          example: 900

    ResumeRef:
      type: object
      required: [resumeId, previewUrl, downloadUrl, updatedAt]
      properties:
        resumeId:
          type: string
          example: RES100001
        previewUrl:
          type: string
          format: uri
          example: https://mock-cdn.skillsmine.com/resumes/RES100001/preview.pdf
        downloadUrl:
          type: string
          format: uri
          example: https://mock-cdn.skillsmine.com/resumes/RES100001/download.pdf
        updatedAt:
          type: string
          format: date-time
          example: "2024-11-01T14:30:00Z"

    # ── Recommended Positions ──────────────────────────────────────────────────
    RecommendedJob:
      type: object
      required: [jobId, title, company, matchScore]
      description: Full job object plus a computed matchScore.
      properties:
        jobId:
          type: string
          example: j001
        title:
          type: string
          example: Senior React Developer
        company:
          type: string
          example: Standard Bank
        location:
          type: string
          example: Johannesburg, Gauteng
        industry:
          type: string
          example: Financial Services
        employmentType:
          type: string
          example: Permanent
        workType:
          type: string
          example: Hybrid
        salaryMin:
          type: number
          example: 70000
        salaryMax:
          type: number
          example: 95000
        salaryRange:
          type: string
          example: R70 000 – R95 000
        description:
          type: string
          example: Build and maintain high-performance React applications.
        requirements:
          type: array
          items:
            type: string
          example: ["5+ years React", "TypeScript proficiency"]
        responsibilities:
          type: array
          items:
            type: string
          example: ["Lead frontend architecture", "Code reviews"]
        skills:
          type: array
          items:
            type: string
          example: [React, TypeScript, Node.js]
        postedDate:
          type: string
          format: date
          example: "2024-10-15"
        status:
          type: string
          example: Open
        applicationCount:
          type: integer
          example: 35
        recruiterId:
          type: string
          example: r001
        matchScore:
          type: integer
          minimum: 0
          maximum: 100
          example: 88

    RecommendedPositionsResult:
      type: object
      required: [candidateId, jobs, total]
      properties:
        candidateId:
          type: string
          example: CAND100001
        jobs:
          type: array
          items:
            $ref: '#/components/schemas/RecommendedJob'
        total:
          type: integer
          example: 6

    # ── Saved Jobs ────────────────────────────────────────────────────────────
    SavedJob:
      type: object
      required: [jobId, title, company]
      properties:
        jobId:
          type: string
          example: j001
        title:
          type: string
          example: Senior React Developer
        company:
          type: string
          example: Standard Bank
        location:
          type: string
          example: Johannesburg, Gauteng
        workType:
          type: string
          example: Hybrid
        employmentType:
          type: string
          example: Permanent
        salaryRange:
          type: string
          example: R70 000 – R95 000
        skills:
          type: array
          items:
            type: string
          example: [React, TypeScript]
        postedDate:
          type: string
          format: date
          example: "2024-10-15"
        savedAt:
          type: string
          format: date-time
          example: "2025-01-10T08:00:00Z"

    SavedJobsResult:
      type: object
      required: [candidateId, jobs, total]
      properties:
        candidateId:
          type: string
          example: CAND100001
        jobs:
          type: array
          items:
            $ref: '#/components/schemas/SavedJob'
        total:
          type: integer
          example: 2

    SaveJobRequest:
      type: object
      required: [jobId]
      properties:
        jobId:
          type: string
          example: j001

    # ── AI Actions ────────────────────────────────────────────────────────────
    AiAction:
      type: object
      required: [actionId, type, label]
      properties:
        actionId:
          type: string
          example: ai-action-001
        type:
          type: string
          enum: [MATCH_SUMMARY, SKILL_GAP, CAREER_NUDGE, SEND_MATCHED_JOBS]
          example: MATCH_SUMMARY
        label:
          type: string
          example: "Your profile is 88% matched to 3 open roles"
        description:
          type: string
          example: "Update your skills to improve match score."
        payload:
          type: object
          nullable: true
          additionalProperties: true

    AiActionsResult:
      type: object
      required: [candidateId, actions]
      properties:
        candidateId:
          type: string
          example: CAND100001
        actions:
          type: array
          items:
            $ref: '#/components/schemas/AiAction'

    # ── CV Upload ─────────────────────────────────────────────────────────────
    CvUploadValidation:
      type: object
      required: [isComplete, missingFields, warnings]
      properties:
        isComplete:
          type: boolean
          example: true
        missingFields:
          type: array
          items:
            type: string
          example: []
        warnings:
          type: array
          items:
            type: string
          example: []

    CvUploadResult:
      type: object
      required: [applicationId, documentId, extractionStatus, uploadedAt]
      properties:
        applicationId:
          type: string
          example: APP-MS-001
        documentId:
          type: string
          example: DOC45231
        extractionStatus:
          type: string
          enum: [NOT_STARTED, IN_PROGRESS, COMPLETED]
          example: COMPLETED
        personalDetails:
          type: object
          properties:
            firstName:
              type: string
              example: Michael
            lastName:
              type: string
              example: Smith
            email:
              type: string
              format: email
              example: michael.smith@email.com
            mobileNumber:
              type: string
              example: "+27821234567"
            location:
              type: string
              example: Johannesburg, Gauteng
            linkedinUrl:
              type: string
              example: https://linkedin.com/in/michael-smith-dev
        careerHistory:
          type: array
          items:
            $ref: '#/components/schemas/CareerHistoryEntry'
        skills:
          type: array
          items:
            type: string
          example: [React, Node.js, TypeScript, AWS, PostgreSQL, GraphQL]
        education:
          type: array
          items:
            type: object
            properties:
              institution:
                type: string
                example: University of the Witwatersrand
              qualification:
                type: string
                example: BSc Computer Science
              year:
                type: integer
                example: 2016
        languages:
          type: array
          items:
            $ref: '#/components/schemas/LanguageEntry'
        validation:
          $ref: '#/components/schemas/CvUploadValidation'
        uploadedAt:
          type: string
          format: date-time
          example: "2025-01-15T10:00:00Z"

    # ── Candidate list ────────────────────────────────────────────────────────
    CandidateDocument:
      type: object
      required: [docId, type, uploadedAt]
      properties:
        docId:
          type: string
          example: doc-001
        type:
          type: string
          example: CV
        uploadedAt:
          type: string
          format: date-time
          example: "2024-10-15T09:00:00Z"

    CandidateEducationEntry:
      type: object
      required: [institution, qualification, year]
      properties:
        institution:
          type: string
          example: University of the Witwatersrand
        qualification:
          type: string
          example: BSc Computer Science
        year:
          type: integer
          example: 2016

    CandidateExperienceEntry:
      type: object
      required: [company, title, from, to]
      properties:
        company:
          type: string
          example: Accenture
        title:
          type: string
          example: Senior Software Engineer
        from:
          type: string
          example: "2020-03"
        to:
          type: string
          example: Present

    CandidateListItem:
      type: object
      required: [candidateId, fullName, email, profileComplete]
      properties:
        candidateId:
          type: string
          example: CAND100001
        fullName:
          type: string
          example: Michael Smith
        email:
          type: string
          format: email
          example: michael.smith@email.com
        phone:
          type: string
          example: "+27821234567"
        location:
          type: string
          example: Johannesburg
        currentTitle:
          type: string
          example: Senior Software Engineer
        currentCompany:
          type: string
          example: Accenture
        experienceYears:
          type: integer
          example: 8
        skills:
          type: array
          items:
            type: string
          example: [React, Node.js, TypeScript]
        education:
          type: array
          items:
            $ref: '#/components/schemas/CandidateEducationEntry'
        experience:
          type: array
          items:
            $ref: '#/components/schemas/CandidateExperienceEntry'
        documents:
          type: array
          items:
            $ref: '#/components/schemas/CandidateDocument'
        languages:
          type: array
          items:
            type: string
          example: [English, Zulu]
        profileComplete:
          type: integer
          minimum: 0
          maximum: 100
          example: 85
        applications:
          type: array
          description: Array of application IDs
          items:
            type: string
          example: [APP-MS-001, APP-MS-002]

    CandidateListResult:
      type: object
      required: [candidates, pagination]
      properties:
        candidates:
          type: array
          items:
            $ref: '#/components/schemas/CandidateListItem'
        pagination:
          $ref: '#/components/schemas/Pagination'

    # ── Recruiter candidate search ─────────────────────────────────────────────
    RecruiterCandidateSearchItem:
      type: object
      required: [candidateId, userId, firstName, lastName, email]
      properties:
        candidateId:
          type: string
          example: CAND100001
        userId:
          type: string
          example: USR100001
        firstName:
          type: string
          example: Michael
        lastName:
          type: string
          example: Smith
        email:
          type: string
          format: email
          example: michael.smith@email.com
        location:
          type: string
          example: Johannesburg
        eeStatus:
          type: string
          nullable: true
          example: African Male
        currentTitle:
          type: string
          example: Senior Software Engineer
        skills:
          type: array
          items:
            type: string
          example: [React, TypeScript, Node.js]
        profileCompleted:
          type: integer
          minimum: 0
          maximum: 100
          example: 85
        matchScore:
          type: integer
          minimum: 0
          maximum: 100
          example: 78

    RecruiterCandidateSearchResult:
      type: object
      required: [candidates, pagination]
      properties:
        candidates:
          type: array
          items:
            $ref: '#/components/schemas/RecruiterCandidateSearchItem'
        pagination:
          $ref: '#/components/schemas/Pagination'

    # ── Recruiter full candidate profile ──────────────────────────────────────
    RecruiterApplicationEntry:
      type: object
      required: [applicationId, jobId, jobTitle, company, currentStage, appliedDate, matchScore]
      properties:
        applicationId:
          type: string
          example: APP-MS-001
        jobId:
          type: string
          example: j001
        jobTitle:
          type: string
          example: Senior React Developer
        company:
          type: string
          example: Standard Bank
        currentStage:
          $ref: '#/components/schemas/InternalStageEnum'
        appliedDate:
          type: string
          format: date
          example: "2024-10-16"
        matchScore:
          type: integer
          minimum: 0
          maximum: 100
          example: 92

    RecruiterCandidateProfile:
      type: object
      required: [candidateId, userId, personalDetails]
      properties:
        candidateId:
          type: string
          example: CAND100001
        userId:
          type: string
          example: USR100001
        accountStatus:
          type: string
          example: ACTIVE
        profileCompleted:
          type: integer
          minimum: 0
          maximum: 100
          example: 85
        personalDetails:
          $ref: '#/components/schemas/PersonalDetails'
        desiredJob:
          $ref: '#/components/schemas/DesiredJob'
        education:
          $ref: '#/components/schemas/EducationRecord'
        experience:
          type: array
          items:
            $ref: '#/components/schemas/ExperienceEntry'
        skills:
          type: array
          items:
            type: string
          example: [React, Node.js, TypeScript, AWS]
        languages:
          type: array
          items:
            $ref: '#/components/schemas/LanguageEntry'
        resume:
          $ref: '#/components/schemas/ResumeRef'
          nullable: true
        applications:
          type: array
          items:
            $ref: '#/components/schemas/RecruiterApplicationEntry'
        matchScore:
          type: integer
          minimum: 0
          maximum: 100
          example: 82
