HTTP/1.1 200 OK
Content-Type: application/json

{
  "id": "{{request.body.id}}",
  "candidateId": "{{request.body.candidateId}}",
  "previousStage": "{{request.body.previousStage}}",
  "newStage": "{{request.body.newStage}}",
  "updatedAt": "{{now 'YYYY-MM-DDTHH:mm:ss'}}Z",
  "message": "Candidate stage updated successfully."
}
