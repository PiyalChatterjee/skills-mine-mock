HTTP/1.1 200 OK
Content-Type: application/json

{
  "user": {
    "id": "{{user.sub}}",
    "email": "{{user.email}}",
    "role": "{{user.role}}",
    "permissions": {{{jsonStringify user.permissions}}}
  }
}
